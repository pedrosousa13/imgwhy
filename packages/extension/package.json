{
  "name": "@imgwhy/extension",
  "version": "0.0.0",
  "description": "Manifest V3 extension. Explain the page you are looking at, and cost it nothing until you click.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@imgwhy/core": "*"
  }
}
